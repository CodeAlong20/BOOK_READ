<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.bitvilltecnologies.bookword">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Authtentications.ProfileActivity" />
        <activity
            android:name=".SearchActivity"
            android:windowSoftInputMode="adjustResize" />
        <activity android:name=".Authtentications.SignupActivity" />
        <activity android:name=".Authtentications.LoginActivity" />
        <activity android:name=".MyBoooks" />
        <activity
            android:name=".NEWBOOK"
            android:windowSoftInputMode="adjustResize" />
        <activity android:name=".Library" />
        <activity
            android:name=".MainActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:theme="@style/FullscreenTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>